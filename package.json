{
  "name": "mereke-openai",
  "version": "1.1.0",
  "private": true,
  "description": "Mereke: contractor matching with server-side OpenAI explanations",
  "engines": { "node": "22.x" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/local.mjs",
    "test": "node tests.cjs && node --test server/recommend.test.mjs",
    "build": "node scripts/check-build.cjs"
  }
}
